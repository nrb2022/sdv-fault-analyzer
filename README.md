# sdv-fault-analyzer
a Python CLI tool that ingests vehicle telemetry CSV data, detects faults using rule-based logic, maps them to DTC codes, and outputs a diagnostic report. Real code, real GitHub repo, real portfolio piece.
